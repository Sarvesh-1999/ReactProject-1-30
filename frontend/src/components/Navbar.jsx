import React from "react";

const Navbar = () => {
  return (
    <nav className="sticky top-0 h-[70px] w-full bg-blue-300 flex items-center justify-between px-3">
      <div className="font-semibold text-3xl">Logo</div>
    </nav>
  );
};

export default Navbar;
